#include <iostream>
#include <string>

int main()
{
    int lines_in_program, number_of_queries;
    std::cin >> lines_in_program >> number_of_queries;
    //ignore the newline that follows cin so that getline doesn't return a blank
    std::cin.ignore();

    std::vector<vector<std::string>> two_dim_vector;
    for(int i =0; i < lines_in_program; i++)
    {



    }

    return 0;
}
